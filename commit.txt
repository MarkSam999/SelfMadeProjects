git commit -m "Created a home page" -m "- Created a home.html and added some content to this page
- In count.html, added the link that redirects to home
-Some of the css code was translated to style element in count.html, to ensure that the home page's display will work responsively, but the count.html will have the same layout" 

