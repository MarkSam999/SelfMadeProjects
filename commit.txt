git commit -m "Described truck types & changed layout" -m "HTML: 
-Added 3 pages, where all of the available types of trucks are described
-Replaced divs with class 'container' by sections
-On each page, I've written the information about its mpg (miles per gallon), maximum weight it can lift, acceleration time, and rent
-Because the links to these pages didn't work, I've slightly changed the html of the home page
-Because of too much features being present, I distributed all of the features by sections, such as Costs & Requirements, Technical Features, and Cargo Features
CSS:
-CSS was changed to ensure that all of the rulesets set to the elements that like to these pages still apply
-Reduced the content's width to improve readability
JavaScript:
-After finding out more about the technical features of different cargo cars, I added 2 new values, changed the values for the maximum load of each type of car and changed the formula of counting the total cost
-Deleted value tax because I found it useless on my imaginary service"